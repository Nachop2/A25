models:
  Country:
    name: string
    relationships:
      hasMany: User
  Post:
    name: string
    user_id: id
    relationships:
        belongsTo: User

controllers:
  # ...
 


seeders: Country, User, Post
